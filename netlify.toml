[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"
  NPM_VERSION = "10"
  # Omit INITDATA_MAX_AGE_SECONDS from secrets scanning - it's a timeout config, not a secret
  SECRETS_SCAN_OMIT_KEYS = "INITDATA_MAX_AGE_SECONDS"

# Redirect all requests to Next.js
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/___netlify-handler"
  status = 200
  force = true

# Environment variables are set in Netlify dashboard
# Don't put secrets here!
